# Reading Log

Project context for Claude Code sessions in this repo.

## Rules
- Keep features small and focused.
- Build only one thing at a time — token cost is real.
- Plain HTML · CSS · vanilla JS. No build step.
- Persist data in `localStorage`. No backend, no accounts, no external APIs.

<!-- FUTURE FEATURE (not yet) -->
<!-- A "reading stats" summary view: total books read, average rating, and
     a count of books per status. Pure client-side computation over the
     localStorage data — no third-party services.
     We turn this into an artifact and build it with goal mode in Block 2.
     Do NOT build it now. -->
